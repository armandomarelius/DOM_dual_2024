// EJERCICIO1 : Crear un formulario que permita validar el correo electrónico 
// si el email no es válido mostrar un mensaje de error dinámico bajo
// el campo de texto

// EJericio2 : dado un array de objetos con datos de usuarios (nombre , email , edad),
// crear una tabla dinámicamente en la que cada fila corresponda a un usuario .
// añade funcionalidad para ordenar las filas por nombre y edad haciendo click
// en los encabezados de las columnas.
// utilizando la lista de usuarios añadir un input para que a medida que busquemos 
// filtre los usuarios por su nombre.

// Ejercicio 3: 
// Crear una lista de elementos que al hacer el botón click sobre unno de ellos debe aparecer un modal
// para confirmar si lo quiero eliminar o no 
// si le doy que si se eliminará de la lista ( hay que componentizar lo modal )


import divForm from "./components/divForm.js";

document.addEventListener("DOMContentLoaded", () => {
    const app = document.getElementById("app");
    // Ejercicio contenedor for,ilario test email 
  //  const divFormulario = divForm();

   // app.appendChild(divFormulario); // <--------- divForm inyectado  

   // ----- Crear tala dinámicamente ---------
   
});


